// import React from 'react';
// import { useSelector } from 'react-redux';
// import JobCard from './JobCard'; // Adjust the path if necessary
// import styles from './ParentComponent.module.css'; // Assuming you have a CSS module for the parent component

// const ParentComponent = () => {
//   const AllJobs = useSelector((state) => state.jobs.jobs);

//   return (
//     <div className={styles.jobList}>
//       {AllJobs.map((job) => (
//         <JobCard key={job._id} job={job} jobID={job._id} id={job._id} onRemove={() => { /* handle remove logic */ }} />
//       ))}
//     </div>
//   );
// };

// export default ParentComponent;
